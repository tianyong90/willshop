const state = {
  product: {}
}

const getters = {}

const actions = {}

const mutations = {
  UPDATE_PRODUCT (state, value) {
    state.product = value
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
